# Get odiff Version

Get odiff Version

## Usage

``` r
odiff_version()
```

## Value

Character string with the odiff version, or `NA_character_` if
unavailable.

## Examples

``` r
if (FALSE) { # \dontrun{
odiff_version()
} # }
```
