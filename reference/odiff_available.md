# Check if odiff is Available

Check if odiff is Available

## Usage

``` r
odiff_available()
```

## Value

Logical `TRUE` if odiff is found and executable, `FALSE` otherwise.

## Examples

``` r
odiff_available()
#> [1] FALSE
```
